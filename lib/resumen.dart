import 'package:flutter/material.dart';
import 'package:teaf_app/resumenUI.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: resumenUI(),
    );
  }
}
